namespace EnumMainType
{
    public enum MainScrollBackType { ScrollToMain, StartToScroll, chToStart, lvToCh, ChallengeToScroll }
    public enum CurrencyType { coin, achieve_point, ad_point } // 코인(인게임), 업적포인트(메인), 광고포인트(메인)
    public enum SoundType { bgm, sfx }
    public enum ProductType { card, artifact }
    public enum ScenarioType { story, challenge }
    public enum ItemSourceType { start, achieve_price, shop, promo }
}
